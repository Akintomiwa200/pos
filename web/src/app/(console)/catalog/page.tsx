export default function CatalogPage() {
  return (
    <section>
      <h1 className="text-2xl font-semibold">Catalog</h1>
      <p className="mt-1 text-neutral-500">
        Items, variants, bundles, modifiers, and kitchen notes — pushed to every
        POS terminal.
      </p>
    </section>
  );
}
