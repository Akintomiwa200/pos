[package]
name = "price-check-desktop"
version = "0.1.0"
description = "Price Check desktop and Android shell"
edition = "2021"
publish = false

[lib]
name = "price_check_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "price-check-desktop"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
