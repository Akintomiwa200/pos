import VerticalPage from "../_vertical";

export default function Page() {
  return (
    <VerticalPage
      kicker="Solutions · Dark kitchen"
      title="Dispatch without a dining room."
      copy="Aggregator tickets and bag labels, still licensed as one till per station."
      points={[
        "Keep the till on a pack station; price check on the line.",
        "HQ still owns items, tax, and the till code.",
        "No table map required — KDS and paid tickets are enough.",
      ]}
    />
  );
}
